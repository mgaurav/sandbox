" syntax highlighting
syntax on

" tab settings
set tabstop=4
set shiftwidth=4
set expandtab

set ignorecase
set smartcase

" ctags; search for tags file recursively up the directory structure
set tags=./tags;/

set path=/usr/local/include,/usr/include

filetype indent on
set linebreak

" Cursor will briefly jump to the matching brace while typing
set showmatch

" wildmenu
set wildmenu
set wildmode=list:full
